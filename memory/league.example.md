# League — standing context

**Placeholder.** These details are filled in automatically by `hal-mary sync`,
which reads the league settings from ESPN and rewrites everything *above* the
sentinel below.

Until the first sync runs, hal-mary knows none of the following, and any advice
that depends on them should say so rather than assume:

- League name, size, and where Caroline's team sits in the draft order
- Scoring type (standard / PPR / half-PPR) and any custom scoring
- Roster slots — how many of each position start, and how deep the bench goes
- Draft type (snake or auction) and the draft date and time
- Waiver rules and the trade deadline

---

<!-- hal-mary:preserve-below -->

## Notes added by hand

Everything below the `hal-mary:preserve-below` sentinel is **preserved verbatim**
by `hal-mary sync`. Write anything here that ESPN does not know and hal-mary
should: house rules, what the commissioner said in the group chat, who trades
fairly, which manager never sets a lineup.

Do not delete the sentinel line — it is what tells the sync where the
machine-written half of this file ends.

_(nothing here yet)_
